[DIRECTORY]
logs_dir = ./logs

[SERVER]
auth_endpoint = http://localhost:8001
username=joshburt
password=secret
secret_key=NOT_NEEDED
algorithm=NOT_NEEDED
issuer=NOT_NEEDED

endpoint = http://localhost:8000
timeout = 30
